# Project Memory

<!--
Canonical PROJECT memory for AI agents. Committed to version control and shared with the
team. Agents read this automatically at the start of a session.

RECORD here: conventions, architecture decisions, build/test/run commands, gotchas,
domain terms — anything a teammate's agent should know.
DO NOT record: secrets, tokens, passwords, personal data, or ephemeral task notes
(use session memory for those).

Preferred way to add an entry:
  {{PYTHON_COMMAND}} {{HOOKS_DIR}}/record_memory.py --scope project --topic "Commands" --text "..."

Keep entries concise and grouped under the headings below. Prefer updating an existing
bullet over adding a duplicate. This header comment is stripped before injection, so it
costs no context tokens.
-->

## Conventions

## Architecture

## Commands

## Gotchas

## Glossary
